{
  "name": "patientor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "dev": "vp run --parallel --log labeled -F server -F web dev",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "check": "vp check",
    "build": "vp run -r build",
    "test": "vp run -r test",
    "typecheck": "vp run -r typecheck",
    "lint": "vp run -r lint",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@oxlint/plugins": "catalog:",
    "@types/node": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.32.1"
}
