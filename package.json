{
  "name": "patientor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp run -r lint && vp run -r test && vp run -r build",
    "dev": "vp run server#dev",
    "build": "vp run -r build",
    "test": "vp run -r test",
    "typecheck": "vp run -r typecheck",
    "lint": "vp run -r lint"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.32.1"
}
